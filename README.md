# WEBSITE Project

> 🤖 Этот проект был автоматически сгенерирован с помощью AI Constructor Bot

## 📝 Описание

<b>ИСХОДНЫЙ ЗАПРОС:</b>
Create a surreal, high-fashion, ultra-tech landing page for "AETHER PROTOCOL" — a liquid-liquidity neobank. 
The design must be "Mind-Blowing", experimental, and cinematic, pushing the boundaries of what's possible with React and CSS.

1. THE AESTHETIC (Obsidian & Liquid Chrome):
* Palette: Background is a grainy "Deep Obsidian" (#050505). The primary accent is "Molten Chrome" (a silver/white gradient with high reflectivity). The "daring" secondary accent is "Solar Flare" (#FF4D00 — a sharp, aggressive blood-orange).
* Atmosphere: Use a heavy "Noise & Grain" filter (5-8% opacity). Implement a "Custom Cursor" that looks like a small liquid blob that deforms when moving fast.
* Typography: Use a high-contrast serif for main headlines (like 'PP Nikkei' or 'Playfair Display' but bold/experimental) mixed with a tiny, sharp Monospace for technical data.

2. INSANE UNIQUE ELEMENTS:

* HERO SECTION: The "Liquid Singularity"
    * Visual: In the center, create a "Liquid Mercury Sphere" (using CSS radial gradients and Framer Motion). It must pulse and "warp" its shape based on the user's scroll position and mouse proximity. 
    * Headline: "WEALTH IN FLUID MOTION." (Text should have a 'Masking' effect, where the Liquid Chrome texture is visible inside the letters).
    * Interaction: On scroll, the sphere should "shatter" into smaller droplets that float into the background.

* THE "GRAVITY" NAVIGATION:
    * The menu items shouldn't just sit there. They should have a "Magnetic" pull toward the cursor. When hovered, they should slightly distort the layout around them as if they have mass.

* SECTION: "The Alchemical Vault"
    * A horizontal scroll section where assets (Gold, BTC, USD) appear as "3D Shards" of glass.
    * As the user scrolls, these shards rotate and "melt" into a single liquid stream that follows the scroll path.

* DATA VISUALIZATION: "The Pulse"
    * Instead of a chart, create a "Heartbeat" line made of glowing Solar Flare particles. It should react to the user's mouse clicks with a ripple effect across the entire screen.


Goal: Create a visual experience that feels expensive, mysterious, and technically superior. The user shouldn't just "read" the site; they should feel like they are interacting with a living digital substance.

<b>УТОЧНЕНИЯ:</b>
🧩 Какие блоки обязательны для лендинга?
→ Герой с Liquid Mercury Sphere
• Горизонтальная секция Alchemical Vault
• Интерактивная визуализация Pulse

⚡ Какие эффекты для Liquid Mercury Sphere наиболее важны?
→ Пульсация при приближении курсора
Деформация при прокрутке

⚡ Какой тип анимации для меню предпочтителен?
→ Искажение вокруг при наведении

🎨 Какие дополнительные элементы стиля важны?
→ Текстура шума и зерна
• Кастомный курсор в виде жидкого пятна



## 🚀 Технологии

- React (для интерактивности и сложных анимаций)  
- Framer Motion (для плавных и сложных анимаций, включая деформацию и пульсацию)  
- Three.js (для 3D-эффектов, таких как "3D Shards" в Alchemical Vault)  
- GSAP (для дополнительных анимаций и контроля временных линий)  
- CSS Modules или styled-components (для стилизации с поддержкой переменных и сложных эффектов)  
- WebGL (опционально, для более продвинутых визуальных эффектов, таких как жидкие текстуры)

## 📁 Структура проекта

- `package.json`
- `index.html`
- `vite.config.js`
- `tailwind.config.js`
- `postcss.config.js`
- `src/main.jsx`
- `src/index.css`
- `src/App.jsx`
- `src/components/IconRegistry.tsx`
- `src/components/SafeIcon.tsx`
- `vercel.json`
- `.gitignore`
- `README.md`

## 🛠️ Установка

```bash
npm install
```

## ▶️ Запуск

```bash
npm start
# или
npm run dev
```

## 📋 План разработки

1.

## 📄 Лицензия

MIT

## 🤖 Создано с помощью

[AI Constructor Bot](https://t.me/construct_ai_bot) - Telegram бот для автоматической генерации проектов с помощью AI
